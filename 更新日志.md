# 背单词 · 更新日志

> 作者：邹光凡 | 起始：2026-08-29
> 记录每次版本发布的功能、bug 修复与优化，按时间倒序。

---

## V1.20 · 2026-09-07 10:14

### 📚 新增「短语本」独立模块

#### 数据
- 独立文件 `phrases.js`，共 410 条短语
  - 初中常用短语 120 条（考纲短语：look forward to、take off、give up…）
  - 高中常用短语 180 条（考纲短语：in terms of、come up with、regardless of…）
  - 开发工具界面短语 110 条（Quick Start、Configuration、Drag and Drop、Pull Request…）

#### 界面
- 首页新增「📚 短语本」卡片，显示总数/已学/过关/掌握度进度条
- 短语本详情页：level 统计（初中/高中/开发各多少条）+ 数量按钮（10/20/50/100/全部）
- 学习卡片：
  - 正面：短语原文 + 🔊发音按钮 + level 标签 + 当前标签
  - 背面：中文释义 + 例句 + 标签按钮（复用 5 个标签：认识/模糊/重点/必背/过关）
  - 倒计时 12s/条（比单词 8s 更宽，短语需要多看一眼）

#### 存储与备份
- 独立 localStorage 键：`wa:p-labels`、`wa:p-studied`、`wa:p-sessions`、`wa:p-totals`、`wa:p-cursor`
- 标签 key 为 `phrase:短语原文`，和单词标签完全独立
- 纳入 `collectData()` 导出 + `importBackup()` 导入恢复

#### 代码改动
- 新增 `phrases.js` 数据文件
- [app.js](file:///Users/zoujiean/TraeWork%20CN/背单词/app.js)：STORE 加短语存储、短语辅助函数（phraseKey/phraseLabelOf/pickPhrases 等）、renderPhraseBook、renderStudy/renderResult/flipCard/markLabel/advance 兼容 phrase 模式、onAction 加 phrase-study/go-phrase、collectData/importBackup 加短语字段
- [style.css](file:///Users/zoujiean/TraeWork%20CN/背单词/style.css)：新增 level-tag 样式
- [index.html](file:///Users/zoujiean/TraeWork%20CN/背单词/index.html)：引入 phrases.js，版本号升到 V1.20

---

## V1.19 · 2026-09-05 08:08

### 🔊 查询结果卡片新增发音按钮

- 在搜索结果卡片的单词旁加了 🔊 按钮，点击调用有道真人发音接口（复用现有 `speakWord` 函数）。
- 查词时可以顺便听一下发音，不用进到学习卡片里。

---

## V1.18 · 2026-09-05 07:47

### 🔧 查词本调整（根据用户反馈）

- **练习不再跳过已认识/过关的词**：不管打了什么标签，查词本里的词都可以拿出来复习、改标签（打了「认识」的词以后也可能忘）。
- **列表只显示前 10 名**：查词本详情页的单词列表，只显示查词次数最多的前 10 个词，避免列表过长。
- **删除「清空查词本」按钮**：历史查词记录保留更有价值，不需要清空。

---

## V1.17 · 2026-09-05 07:32

### 🔍 查词 + 查词本（工作中碰到的词，专门练）

#### 功能设计
- **首页常驻搜索框**：粘贴单词 → 回车/点搜索 → 跨 5 个分类（初中/高中/四级/六级/专业）精确匹配（大小写不敏感）
- **命中后自动收入查词本**：每查一次 count+1，记录到 `wa:lookups`
- **结果卡片可直接打标签**：显示单词、音标、释义、所属分类、当前标签、查词次数；点标签按钮同步到原分类（key 为 `cat:word`，全局唯一）
- **未收录提示**：5 个分类都没有的词，显示「词库未收录」，不收入查词本

#### 查词本（第六分类）
- 首页新增「🔍 查词本」卡片，显示已收集词数
- 详情页：按查词次数降序列表（最不熟的在前），每个词显示释义、所属分类、当前标签、查词次数
- 打了「认识/过关」的词列表淡化显示，练习时**自动跳过**
- 可选 10 / 20 / 50 / 100 / 全部 开始练习
- 支持「清空查词本」（只清查词记录，不影响标签和学习进度）

#### 查词本练习
- 抽词顺序：按查词次数从多到少（最不熟的先练）
- 卡片上额外显示：所属分类（彩色）+ 当前标签 + 查词次数
- 打标签全局同步：查词本里改标签 = 原分类里改标签
- 结果页"再背 N 个"走查词本抽词，"返回"回查词本页

#### 数据与备份
- 存储：`wa:lookups` → `{ "cat:word": { count, lastAt } }`
- 纳入 `collectData()` 导出 + `importBackup()` 导入恢复
- 代码改动：[app.js](file:///Users/zoujiean/TraeWork%20CN/背单词/app.js) 新增 searchWord / recordLookup / pickFromLookups / renderLookup / doSearch 等函数；renderStudy / markLabel / flipCard / renderResult 兼容 lookup 模式；[style.css](file:///Users/zoujiean/TraeWork%20CN/背单词/style.css) 新增搜索框/查词本样式

---

## V1.16 · 2026-09-04 20:27

### 🏷️ 标签微调：眼熟 → 模糊

- **改动**：`fuzzy` 标签的中文显示名从「眼熟」改为「模糊」。
- **影响范围**：仅改 [app.js](file:///Users/zoujiean/TraeWork%20CN/背单词/app.js#L21) `LABELS.fuzzy.name` 一行；key / 颜色 / 权重完全不变，已存标签数据**零影响**。

---

## V1.15 · 2026-09-02 18:55

### 🏷️ 标签重命名（显示名称调整，数据不受影响）

- **改动**：5 个标签的中文显示名称调整如下：

| key（不变） | 旧名称 | 新名称 | 颜色/权重（不变） |
|---|---|---|---|
| `know` | 记得 | **认识** | 绿 #34c759 / 0.3 |
| `fuzzy` | 不太熟悉 | **眼熟** | 黄 #f5a623 / 3 |
| `key` | 重点 | **重点**（不变） | 橙 #ff6b35 / 4 |
| `must` | 必背 | **必背**（不变） | 红 #e74c3c / 5 |
| `graduate` | 毕业 | **过关** | 蓝 #4f7cff / 0 |

- **原理**：代码内部用 key（`know` / `fuzzy` / `key` / `must` / `graduate`）做逻辑判断，显示名称只是 `LABELS` 对象的 `name` 字段。只改 `name` 值，**不影响 localStorage 已保存的标签数据**。
- **同步更新**：[app.js](file:///Users/zoujiean/TraeWork%20CN/背单词/app.js#L19-L25) 中 14 处硬编码旧名称（注释、按钮文案、alert/confirm 提示文本）全部替换为新名称；[DESIGN.md](file:///Users/zoujiean/TraeWork%20CN/背单词/DESIGN.md#L119-L125) 标签系统表格、过关机制描述、抽词逻辑描述同步更新；版本历史记录保留原始名称。
- **后续改名同样简单**：只需改 `app.js` 第 20-24 行的 `name` 值 + index.html 版本号即可。

---

## V1.14 · 2026-09-01 08:57

### ⚙️ 参数调整：倒计时每词时长 5s → 8s（首遍过单词专用）

- 触发背景：首遍过单词表时需要宽松节奏认音标、看释义、偶尔点发音，3s/5s 都偏紧；后续第二遍及复习时再逐步调回。
- 修改文件：[app.js](file:///Users/zoujiean/TraeWork%20CN/背单词/app.js#L214-L216) 第 215 行 `SEC_PER_WORD` 常量。
- 对应各档目标时间（8s/词）：

| 数量 | 目标时长 |
|---|---|
| 10 | 1 分 20 秒 |
| 20 | 2 分 40 秒 |
| 50 | 6 分 40 秒 |
| 100 | 13 分 20 秒 |
| 200 | 26 分 40 秒 |

- 后续调整只需改这一行常量，代码其他部分无需改动；调参时别忘了同步升级 `index.html` 的版本号参数触发浏览器重新加载。

---

## V1.13 · 2026-09-01 07:31

### 🐛 Bug 修复：🔊 双音源叠加（同时出男声+女声）

- **现象**：用户点击 🔊 发音按钮时，同一个单词会先后或同时听到两个人声（有道女声 + Web Speech 男声），之前版本只有有道女声。
- **根因**：[speakWord](file:///Users/zoujiean/TraeWork%20CN/背单词/app.js#L698-L743) 里有道分支的回退逻辑写得过宽：
  ```js
  audio.play().catch(() => speakBySpeech(word));   // ❌ 任何 Promise 失败都会回退
  ```
  Chrome 对 `audio.play()` 返回的 Promise 有多种 reject 场景（自动播放策略拦截、跨域 audio 元素的 play 被延后、音频元素被 GC 等），其中大多数场景**实际上有道已经开始播放/加载成功了**，只是 Promise 被拒绝，于是 `.catch()` 又调用了一次 `speakBySpeech`，两个音源同时播。
- **修复**：
  1. **收紧回退触发**：只有 `audio.onerror`（真正的网络/资源 4xx/5xx 或 CORS 失败）才调用 `speakBySpeech(word)`；`audio.play().catch()` 只对明确的 `NotSupportedError` 兜底。
  2. **去重锁**：加 `fallbackFired` 布尔变量，onerror 和 play.catch 之间互斥，确保极端情况下也不会触发两次回退。
  3. **清残留**：每次发声前先 `speechSynthesis.cancel()`，避免上一轮 Web Speech 还没说完叠加到新的有道发音上；同时清掉旧 `_audio` 的 onended/onerror 回调，防止触发陈年事件。
  4. **取消旧 Audio 更彻底**：stop 旧音频时除了 `pause()`、`src=''` 再加 `onended=null`、`onerror=null`。
- **验证预期**：有网状态下点 🔊 → 只有有道一个女声；断网 → 才会降级到 Web Speech（男声/系统语音），不会重复。

### ⚙️ 参数调整：倒计时每词时长 3s → 5s

- `SEC_PER_WORD` 常量从 3 改为 5，节奏更从容。对应各档目标时间更新为：

| 数量 | 目标时长 |
|---|---|
| 10 | 50 秒 |
| 20 | 1 分 40 秒 |
| 50 | 4 分 10 秒 |
| 100 | 8 分 20 秒 |
| 200 | 16 分 40 秒 |

---

## V1.12 · 2026-09-01 07:00

### ✨ 新功能：学习倒计时（紧迫感 · 默认 3 秒/词）

**触发背景**：用户希望背单词时有一个时间约束带来的紧迫感，每天做几组更专注。每词的时长保持"有紧迫感但不抢"的节奏。

#### 核心设计

- **时长节奏**：默认 `SEC_PER_WORD = 3 秒/词`，对应数量 → 目标时间：
  | 数量 | 目标时长 |
  |---|---|
  | 10 | 30 秒 |
  | 20 | 1 分钟 |
  | 50 | 2 分 30 秒 |
  | 100 | 5 分钟 |
  | 200 | 10 分钟 |
- 第一期先不加速度切换，保持单一节奏（用户方案已确认）。

#### 界面

- **位置**：学习卡片顶部 topbar 的**最右侧**，不遮挡退出按钮和进度文字。
- **组件**：
  1. **圆形进度环**（52×52px SVG）：白色背景圆环 + 彩色进度弧线
  2. **数字倒计时**（显示在圆环中心，`mm:ss` 格式，12px 粗体）
  3. **⏸ 手动暂停按钮**（30×30px 圆形，圆环右边）
- **颜色变化**（按剩余时间比例）：
  - 剩余 > 60% → 绿色 (`#34c759`)
  - 剩余 30–60% → 橙色 (`#f29a3d`)
  - 剩余 < 30% → 红色 (`#e74c3c`)，**带脉冲动画**（一圈红色光晕脉冲跳动，强提醒节奏紧）

#### 逻辑实现

1. **启动**：每次 `startSession()` 时调用 `timerReset(词数)` 启动计时器。
2. **推进**：250ms 一个 `setInterval` tick，用 `Date.now()` 差值推进剩余时间（避免 setInterval 本身不精确）。
3. **暂停机制（双保险）**：
   - 手动：点 ⏸ 按钮暂停，⏸→▶，再点继续。
   - 自动：监听 `document.visibilitychange`，切到其他标签/最小化浏览器 → 自动暂停；切回来 → 自动恢复。防"偷偷耗时间"。
4. **时间到的处理**：
   - 弹 alert："⏱ 时间到！已完成 X/Y，剩余 Y-X 不算学过"
   - 剩余词**不打标签、不算已学**（studied 不 +1），下次抽词仍会遇到
   - 直接跳结果页，不算一轮 session
5. **正常完成（背完最后一个）**：`advance()` 里累计 `timer.totalElapsed += (totalMs - remainMs)`，停 timer，再走原流程（sessions++、checkin、persist、跳结果页）。

#### 结果页新增"用时"统计

在标签分布 dist 块顶部新增一行：
```
⏱ 实际 00:42 / 目标 01:00   平均每词 2.10 秒
```
帮用户了解自己的速度。

#### 代码改动

- `app.js` 新增：
  - `SEC_PER_WORD`、`TIMER_CIRC` 常量
  - `timer` 状态对象（totalMs/remainMs/startedAt/paused/autoPaused/id/totalElapsed）
  - `timerReset(wordCount)`、`timerStop()`、`timerTick()`
  - `fmtTime(ms)`、`timerRender()`（圆环 SVG + 颜色 + 数字）
  - `timerTogglePause()`（手动暂停/继续）
  - `timerOnVisibility()`（切页自动暂停/恢复）
  - 事件委托加 `case 'timer-pause'`
  - 启动时加 `document.addEventListener('visibilitychange', timerOnVisibility)`
  - `renderStudy` topbar 右栏加 `.timer-wrap` 容器
  - `route()` 结尾 study 页 route 完后 `timerRender()` 立即显示圆环
  - `advance()` 全部背完分支加 totalElapsed 累计、停 timer
  - `renderResult` 加⏱用时行
- `style.css` 新增：`.card-top`、`.timer-wrap`、`.timer-circle`、`.timer-text`、`.timer-pause-btn`、`@keyframes pulse`。

#### 影响范围

只动背单词的学习流程，不影响数据结构、不影响抽词逻辑、不影响标签和毕业机制。新增变量全在内存里，没改 localStorage key。

---

### 🔧 配套优化：静态资源加版本号参数，彻底绕过浏览器缓存

- **背景**：V1.12 开发完成后用户反馈看不到倒计时组件，排查发现 Chrome 静态缓存了旧的 app.js / style.css，导致新代码没加载进来。每次版本更新都要手动 ⌘+Shift+R 硬刷新，体验很差。
- **方案**：在 [index.html](file:///Users/zoujiean/TraeWork%20CN/背单词/index.html) 里给所有本地静态资源 URL 加上 `?v=版本号` 查询参数：
  ```html
  <link rel="stylesheet" href="style.css?v=1.12">
  <script src="data.js?v=1.12"></script>
  <script src="app.js?v=1.12"></script>
  ```
- **原理**：浏览器按 URL 做缓存 key，版本号变了 URL 就变了，浏览器认为是"新文件"→ 自动下载最新代码，完全绕过旧缓存。
- **约定**：以后每次升级版本时，index.html 里的三处 `?v=X.XX` 都要同步改成新版本号（配合 DESIGN.md / 更新日志.md 的版本号）。

---

## V1.11 · 2026-08-30 11:22

### 🐛 Bug 修复：游标顺序推进仍出现重复抽词

- **现象**：V1.04 改为游标顺序推进后，用户反馈仍然遇到之前看过的词，怀疑抽词逻辑未生效。
- **根因**：`pickWords` 第 130 行每次调用都执行 `unlearned.sort((a, b) => (b.hit ? 1 : 0) - (a.hit ? 1 : 0))`，但：
  1. **hit 是布尔值（true/false），不是频次数字**，排序只看 hit 有无
  2. **JavaScript Array.sort 是不稳定排序**（ES2019 前不保证相同元素相对顺序），hit 相同的词内部位置每次都变
  3. 结果：游标指向的是 unlearned 数组的位置 index，但数组顺序每次都变 → 同一位置 index 对应不同的词 → **重复抽词**
- **修复**：删掉每次 pickWords 都 sort 的代码。data.js 里的词已按 hit 排好序（hit:true 在前），保持原始顺序即可，游标就稳定了。
- **验证**：修改后连续抽 3 轮 20 词，未学池顺序不变，游标从 0→20→40→60 严格推进，无重复。
- **影响范围**：仅删一行 sort，其他逻辑不动；游标持久化不受影响。

---

## V1.10 · 2026-08-30 11:13

### ⚡ 优化：去掉翻面自动朗读，改为手动触发

- **背景**：V1.08 加发音功能时设计了"翻面自动朗读一次"（200ms 延迟），意图是"看单词→读发音→看中文"三位一体记忆链。但用户反馈：大多数时候想安静背词，只有记不清发音时才需要听，自动朗读在安静环境下造成干扰。
- **修改**：删除 `flipCard()` 函数内的自动朗读调用（原代码：`setTimeout(() => speakWord(currentWord.word), 200)`）。
- **现状**：只有点击 🔊 按钮才会触发发音，翻面、翻页、下一张均不自动朗读。
- **影响范围**：仅删一行代码，不动 speakWord() 函数和其他路由/样式。

---

## V1.09 · 2026-08-30 10:59

### 🐛 Bug 修复：🔊 发音静默无声音

- **现象**：V1.08 上线后，点击 🔊 按钮和翻面自动朗读都没有声音，控制台无报错但也无声音输出。
- **排查过程**（逐步定位）：
  1. 先怀疑浏览器缓存 → ⌘+Shift+R 硬刷新，音标显示了但仍无声
  2. 控制台手动 `speechSynthesis.speak(new SpeechSynthesisUtterance('hello'))` → 也无声
  3. 终端 `say "hello test"` → **有声音！**（Mac 语音合成本身正常）
  4. 确认系统声音设为"婷婷"（中文语音），Chrome 的 Web Speech API 找不到匹配的英语 voice → 静默
  5. 尝试手动指定 `voice.name='Fred'`（Mac 自带美式英语）→ Chrome 找到 Fred 但仍静默（macOS 上 Chrome 的 speechSynthesis 已知 bug）
  6. 去掉 `cancel()` → 出现 `error: 'canceled'`（Chrome bug：cancel() 会 cancel 紧接着 speak() 的 utterance）
- **根因**：Chrome 在 macOS 上的 Web Speech API（speechSynthesis）存在静默 bug——voice 能找到、speak() 能调用、但就是不出声。这是 Chrome 内部实现问题，非代码可解。
- **修复**：彻底抛弃 Web Speech 作为主方案，改用**有道词典真人发音接口**（`https://dict.youdao.com/dictvoice?type=2&audio=单词`），返回真人 MP3 音频。Web Speech 降级为离线兜底。
- **双方案架构**：
  - 有网（`navigator.onLine`）→ 优先有道真人发音（MP3，音质好，美式 type=2）
  - 有道加载失败（被墙等）→ 自动退回 Web Speech API
  - 离线 → 直接用 Web Speech API 兜底
- **影响范围**：`speakWord()` 函数完全重写，新增 `speakBySpeech()` 兜底函数；去掉启动时 `warmupVoices()` 调用；其他代码（按钮、路由、CSS）不动。

### 🔍 Chrome Web Speech API Bug 历史调查

排查过程中确认此为 Chromium 项目长期已知问题，汇总如下：

| Bug | 年限 | 状态 | 来源 |
|---|---|---|---|
| cancel() 会 cancel 紧接着 speak() 的 utterance | **至少 7 年**（Firefox Bug 1522074 2019 年打开，Chrome 同样存在） | Open，无修复时间表 | [bugzilla.mozilla.org/1522074](https://bugzilla.mozilla.org/show_bug.cgi?id=1522074) |
| cancel()/speak() 队列坏状态 | **11 年**（Firefox Bug 1150315 2015 年打开，Firefox 40 修复但 Chrome 未修） | Firefox 已修复，Chrome 未修 | [bugzilla.mozilla.org/1150315](https://bugzilla.mozilla.org/show_bug.cgi?id=1150315) |
| pause()/resume() 完全坏掉 | 多年 | 公开 Chromium bug，无修复时间表 | [sonorus 项目文档](https://github.com/0xSiddhant/sonorus/blob/main/docs/web-speech-api-limitations.md) |
| onend/onstart 事件不触发 | 多年 | Chrome/Edge 已知问题 | [hermes-webui#3983](https://github.com/nesquena/hermes-webui/issues/3983) |
| macOS 上 volume 设置无效 | 多年 | Chrome 忽略 utterance.volume | sonorus 项目文档 |
| **我们遇到的静默问题** | **≥7 年** | **Chromium 已知 bug，无修复时间表** | 综合多个来源 |

- **结论**：此 bug 至少存在 7-11 年，Chromium 项目明确标注 "no timeline for a fix"（没有修复时间表）。**不太可能在 Chrome 未来版本中自动修复**。Web Speech API 在 Chrome 上整体不可靠，有道真人发音接口是当前最稳妥的方案。
- **替代方案（如有道被墙或未来 Web Speech 修复）**：

| # | 方案 | 原理 | 优点 | 缺点 | 实现难度 |
|---|---|---|---|---|---|
| 1 | **Safari 浏览器** | Safari 的 Web Speech API 在 macOS 上正常工作（不会静默） | 零联网、零代码改动、系统级音质 | 需要换浏览器；Safari 部分 Chrome API 不支持（如 File System Access） | ⭐ 零代码 |
| 2 | **Edge 浏览器** | Edge 的 Web Speech API 相对可靠（基于 Chromium 但语音管线不同） | 零联网、和 Chrome 操作习惯一致 | 仍可能有其他 Web Speech bug；需要换浏览器 | ⭐ 零代码 |
| 3 | **预加载 MP3 到本地** | 用 npm 包 `google-tts-api` 或有道接口批量下载 6722 词的 MP3，存到 `word-audio/` 目录，data.js 加 `audioUrl` 字段 | 离线可用、真人发音、一次下载永久用 | 占空间（6722 × ~50KB ≈ 330MB）；需定期重下载新术语；文件多管理麻烦 | ⭐⭐⭐ 中 |
| 4 | **本地 TTS 模型（Web Audio + Piper）** | 用 WASM 格式的 Piper 模型（Rhubarb Lip Sync 团队出品），~50-200MB 本地推理，11 种英语语音可选 | 完全离线、低延迟、音质接近真人、可定制语音 | 模型体积大（首次加载慢）；需引入 WebAssembly；实现复杂 | ⭐⭐⭐⭐⭐ 高 |
| 5 | **有道接口 + CDN 缓存当前方案** | 当前实现：`dict.youdao.com/dictvoice?type=2&audio=单词`，返回真人 MP3 | ✅ 已实现、音质好、零下载、国内访问快 | 需联网；理论上有道可能屏蔽高频请求（但概率低） | ✅ 已实现 |

> **推荐优先级**：当前方案（有道）→ 方案 1（Safari，零成本切换）→ 方案 3（预加载 MP3，彻底离线）→ 方案 4（本地模型，极客向）。方案 2（Edge）和 Chrome 同属 Chromium 内核，Web Speech 静默问题可能同样存在，优先级最低。

---

## V1.08 · 2026-08-30 10:17

### ✨ 新功能（双发布）

#### ① 音标全量补全（开发计划 1 号）

- **背景**：初中/高中/专业音标原为空（只有四六级自带），无法辅助发音学习。
- **数据源**：ECDICT 开源词库（[skywind3000/ECDICT](https://github.com/skywind3000/ECDICT)），77 万词带音标，下载到 `单词资料/ecdict.csv`（65MB）。
- **实现**：新增 [build-phonetic.js](file:///Users/zoujiean/TraeWork%20CN/背单词/build-phonetic.js)，三步匹配：精确匹配 → 小写匹配 → 去词形变化（-s/-es/-ed/-ing/ies→y）兜底；只补 `phonetic` 为空的词，不动已有数据。
- **覆盖率**：初中 98.5% / 高中 97.3% / 四级 99.8% / 六级 100% / 专业 85.5% → **合计 97.2%（6536/6722）**。专业缺失多为 AI/CS 新术语 ECDICT 未收录（如 GAN、transformer 等），属合理缺失。
- **影响范围**：data.js 文件被覆盖更新（只改 phonetic 字段，其他保留）；后续若有新词资料重跑 build-data.js 再跑 build-phonetic.js 即可。

#### ② 🔊 发音功能（开发计划 3 号 · 重点标红）

- **技术方案**：浏览器原生 Web Speech API（`speechSynthesis`），零外部服务、零音频文件、零联网。
- **触发**：① 卡片正面/背面各一个 🔊 圆形蓝色渐变按钮，点击调 `speechSynthesis.speak(new SpeechSynthesisUtterance(word))`；② **翻面自动朗读一次**（延迟 200ms），强化"看单词→读发音→看中文"三位一体记忆链。
- **参数**：`lang='en-US'`（美式英语）、`rate=0.9`（略慢便于辨识）、`pitch=1`，调用前 `cancel()` 清队列防叠加。
- **CSS**：`.speak-btn` 圆形 52×52，蓝色渐变 `#60a5fa → #2563eb`，立体投影 + 内高光，`:hover` 上浮+放大 1.05，`:active` 回弹归位。
- **兼容性**：Chrome/Edge/Safari 全支持，Mac 系统自带 Samantha 等英文语音；不支持时静默返回不影响学习。
- **影响范围**：app.js 加 `speakWord()` 函数 + `case 'speak'` 路由 + flipCard 内自动读；style.css 加 `.speak-btn` 样式；data.js 音标字段填充。

---

## V1.07 · 2026-08-30 10:01

### ✨ 新功能

#### 抽词数量新增 100 / 200 两档，带立体渐变按钮

- **背景**：原 10/20/50 三档（白底灰边），状态好时想加量冲刺没有更大量级可选。
- **新增**：
  - **100 词**：绿色渐变（`#34d399 → #059669`）+ 投影 + 内高光，hover 上浮 2px、阴影加深；含义"状态不错冲刺用"
  - **200 词**：紫色渐变（`#a78bfa → #7c3aed`）+ 投影 + 内高光，同款立体动效；含义"状态爆棚专注爆发"
  - 配色递进：白底 → 绿 → 紫，量级递增一目了然
- **立体感实现**：`linear-gradient(180deg, ...)` 上浅下深模拟光照 + `box-shadow` 外投影 + `inset 0 1px 0 rgba(255,255,255,0.25)` 顶部内高光 + `:hover translateY(-2px)` 上浮 + `:active translateY(0)` 回弹
- **应用位置（3 处）**：
  - 分类页"选择数量开始学习"行：10/20/50/100/200 五档
  - 复习页"数量"行：10/20/50/100/200 五档（含选中态 active 加深）
  - 结果页"再背"按钮组：20/50/100/200 四档
- **新增 CSS class**：`.num-btn-pro`（100 绿）、`.num-btn-max`（200 紫），含 `:hover/:active/.active` 四态
- **影响范围**：纯 UI 增强，不动数据结构和抽词逻辑；`data-action="study" data-n="100/200"` 复用既有 `startSession(cat, pickWords(cat, n))` 流程，无新代码路径

---

## V1.06 · 2026-08-30 07:04

### 🐛 Bug 修复（备份专项验证发现）

#### ① collectData 漏导出 cursor 字段 → 导入恢复后游标归零、抽词从头开始

- **触发场景**：用户导入备份恢复学习记录，5 分类抽词游标（junior:40 → 0）被清零，导致未学词从第一个词重新开始抽，已推进的 40 个词可能再遇到（若已打标签则不会，但若只是翻面算已学的会重复）。
- **根因**：`collectData()` 函数内没有把 `STORE.cursor` 纳入导出对象，8 个字段缺 1 个关键持久化项。
- **修复方案**：[app.js collectData](file:///Users/zoujiean/TraeWork%20CN/背单词/app.js#L692-L704) 新增 `cursor: STORE.cursor`，字段数从 8 增到 9。
- **验证**：Node 模拟写 10 份备份，每份 `cursor` 5 分类齐全且 junior 值正确。

#### ② doAutoBackup 写入顺序错误：先 collectData → 后 count+1 → 备份文件内 count 比实际少 1

- **触发场景**：第 N 次备份成功后，用户打开备份文件看 `autoBackup.count`，显示 N-1（少 1）。备份内数据和 STORE 内实际状态不一致。
- **根因**：`doAutoBackup` 先 `write(JSON.stringify(collectData()))`，再 `count+1`；collectData 取到的是 +1 前的旧值。
- **修复方案**：将 `STORE.autoBackup.lastAuto = 新时间` 和 `count+1` 前置到 `collectData()` 之前，保证备份内容自洽（count 值 = 本次次数、lastAuto = 本次备份时间）。
- **验证**：第 10 次备份 → 文件内 `autoBackup.count = 10`（修复前为 9）✅。

#### ③ importBackup 漏恢复 cursor

- **修复**：`importBackup` 新增 `if (data.cursor) STORE.cursor = data.cursor;`，和 collectData 新增字段配套。

---

## V1.05 · 2026-08-30 06:57

### ⚡ 优化

#### 自动备份增加"完成一轮后检查"触发点，防止当天备份窗口期错过

- **触发场景**：启动 App 时距上次备份 < 24h（不触发备份），但当天背了多轮、距上次实际已超 24h，因再也不检查时间而一整天未备份成功，新数据裸奔。例如：昨晚 20:09 备份 → 今天 15:00 打开（17h 不触发）→ 用到 22:00 关浏览器（期间已 26h 但没再检查），当天 50 新词没备份。
- **修复方案**：`advance()` 函数在完成一轮（跳转到结果页前）额外调用一次 `checkAutoBackup()`。该函数内部仍保留 `now - last >= 24h` 判断，不到时间直接 return，**一天最多写 1 份文件，备份次数零增加**。
- **效果**：启动时 + 每轮完成时，两个时间点双重检查。即使启动太早错过，也会在当天完成第一轮跨越 24h 时立即备份。
- **影响范围**：无副作用，不增加写文件频率。

---

## V1.04 · 2026-08-30 06:44

### 🐛 Bug 修复

#### 抽词逻辑：滑动窗口随机取词导致未学词跨轮次重复出现

- **触发场景**：在一个分类（如初中 1886 词）连续背多轮时，上一轮背过的新词会在下一轮或下几轮再次出现，推进速度极慢。
- **根因**：`pickWords` 采用"随机 startIdx + 连续 slice"的滑动窗口从 unlearned 池取词。未学池随着每轮完成逐渐收缩（已打标签的词离开 unlearned），导致相邻轮次的 startIdx 所取的窗口在未学池收缩后位置重叠，同一个词可能被两个不同的 startIdx 窗口命中。
- **修复方案**：改为游标顺序推进（`STORE.cursor[cat]`）。游标初始化从 0 开始，每次取 unlearned 池下标 `[cursor, cursor+n)` 的连续 n 个词，取完后 `cursor += n`；若游标到末尾超出则回到 0 从头扫（此时 unlearned 池已变小，因为部分词被打过标签离开了）。完全去随机，确保未学词按频次降序不回头地依次推进。
- **影响范围**：只影响未学词（未进入 `STORE.studied` 的词）的抽取顺序。已打标签的词、已学数据、标签、毕业状态均不受影响，无需重做任何已有进度。

### ⚡ 优化

#### 翻面即算已学，防止中途退出导致词反复出现

- **触发场景**：背到一半关闭浏览器或切页面，某些词已翻面看了答案但没打标签——这些词不会进入 `STORE.studied`，下一轮还会当"未学词"抽到，像新的一样重来。
- **修复方案**：`flipCard` 翻面时，若当前词尚未记录 studied，则写一条 `STORE.studied[key] = 1`（不累加，不影响标签判断），后续退出不会导致该词重回未学池。真正打标签时 `markLabel` 正常 `+1`，两者并存。
- **影响范围**：正向无害。studied 仅用于"是否算已学过一遍"的判定，不掌握度计算（掌握度仍看标签）。

---

## V1.03 · 2026-08-29 19:48

### ✨ 新功能

- 掌握度展示：首页每分类卡片加掌握度百分比 + 彩色进度条 + 目标刻度线
- 统计页每分类展示：掌握度百分比、档位（起步/过半/良好/优秀/精通）、目标值、距目标差几个词
- 规则：掌握度 = (毕业词 + 记得词) ÷ 总词数 × 100%，实时计算
- 颜色 5 档：<50% 红 / 50-70% 橙 / 70-85% 黄 / 85-95% 蓝 / ≥95% 绿
- 目标值：初中 99% / 高中 95% / 四级 90% / 六级 90% / 专业 95%

### 🐛 Bug 修复

- 统计页"已毕业"之前错误显示为"记得"计数，改为正确读取毕业标签数

---

## V1.02 · 2026-08-29 20:15

### ⚡ 优化

- 自动备份改为目录滚动多份模式：每次写新文件（带时分），保留最近 7 份，超出删最旧
- 频率从 7 天改为 1 天，最多丢失 1 天数据
- 使用 `showDirectoryPicker` 授权一个专门备份目录

---

## V1.01 · 2026-08-29 19:48

### ✨ 新功能

- 备份与恢复：手动导出 JSON 到下载目录
- 自动备份：Chrome File System Access API，选固定位置每周写一次
- 导入备份：选 JSON 文件覆盖恢复所有学习记录

---

## V1.0 · 2026-08-29

### ✨ 初始版本

- 五分类词库：初中 / 高中 / 四级 / 六级 / 专业（共 6722 词），已做跨分类分层去重
- 卡片学习模式：正面英文 → 翻面看中文 → 5 标签（记得 / 不太熟悉 / 重点 / 必背 / 毕业）
- 选数量：10 / 20 / 50
- 复习模式：按标签筛选复习
- 毕业机制三件套：毕业（彻底过滤） / 复习毕业词 / 重置毕业
- 统计页：每分类词数 / 已学 / 轮数 / 各标签数
- 连续打卡：完成一轮即打卡，显示连续天数
- 数据持久化：全部存 localStorage

# 背单词 App 设计文档

| 项目 | 内容 |
|---|---|
| 作者 | 邹光凡 |
| 时间 | 2026 年 8 月 29 日 |
| 版本 | V1.19 |
| 目录 | `/Users/zoujiean/TraeWork CN/背单词` |

## 版本历史

| 版本 | 时间 | 升级内容 |
|---|---|---|
| V1.0 | 2026-08-29 | 初始版本：五分类词库（初中/高中/四级/六级/专业）、卡片学习、5 标签（记得/不太熟悉/重点/必背/毕业）、毕业机制（毕业池/复习/重置）、统计与连续打卡。初始版本未记录精确到分钟的时间。 |
| V1.01 | 2026-08-29 19:48 | 新增备份与恢复：手动导出（wordapp-manual-日期.json）、自动备份（File System Access API，每周触发，wordapp-autobackup-日期.json）、导入恢复、立即备份。新增 IndexedDB 持久化 fileHandle。 |
| V1.02 | 2026-08-29 19:48 | 自动备份升级为目录滚动模式：showDirectoryPicker 授权备份目录；每次新建带时分时间戳文件不覆盖；滚动保留最近 7 份，超出删最旧；频率从 7 天改为 1 天；文件名 wordapp-autobackup-YYYYMMDD-HHMM.json。解决单文件覆盖丢数据风险。 |
| V1.03 | 2026-08-29 21:07 | 新增掌握度展示：掌握度=(毕业词+记得词)/总词数×100%；5 档颜色（红/橙/黄/蓝/绿）；各分类目标值（初中99%/高中95%/四级90%/六级90%/专业95%）；首页卡片显示掌握度百分比+彩色进度条+目标刻度线；统计页显示掌握度详情+距目标差几个词；实时计算随打标签更新。 |
| V1.04 | 2026-08-30 06:44 | ① 修复抽词重复 bug：pickWords 从"随机 startIdx 滑动窗口"改为"STORE.cursor[cat] 游标顺序推进"，未学词按频次降序不回头依次取，推进速度不重叠不跳段；② 翻面即算已学：flipCard 首次翻面时写 studied[k]=1（之后 markLabel 正常+1），防中途退出导致词仍在未学池反复出现；③ 新增 更新日志.md + 首页「📝 更新日志」入口 + #/changelog 路由（markdown 渲染）；④ STORE 新增 cursor 字段并持久化（localStorage wa:cursor）。 |
| V1.05 | 2026-08-30 06:57 | 自动备份新增"完成一轮后检查"触发点（advance() 跳结果页前再 checkAutoBackup 一次），弥补"启动时距上次<24h 不备份、整天使用但当天新数据没备份"的漏洞；内部判断仍为满 24h 才写，备份次数零增加。 |
| V1.06 | 2026-08-30 07:04 | 备份专项验证与修复：① collectData 新增 cursor 字段导出（之前漏掉游标状态，导入恢复后游标归零导致从头抽词）；② importBackup 新增 cursor 还原；③ doAutoBackup 将 autoBackup.count+1/lastAuto 更新前置到 collectData 之前（之前先写备份再+1导致备份文件内 count 比实际少1，自洽性差）；备份内容字段从8个增到9个，与localStorage wa:cursor持久化一致。 |
| V1.07 | 2026-08-30 10:01 | 抽词数量新增 100/200 两档立体渐变按钮（100 绿色渐变冲刺用、200 紫色渐变爆发用），分类页/复习页/结果页三处入口同步更新；新增 CSS class `.num-btn-pro` / `.num-btn-max` 含 hover 上浮 + active 回弹 + active 加深四态；纯 UI 增强无逻辑改动。 |
| V1.08 | 2026-08-30 10:17 | ① 音标全量补全：用 ECDICT 开源词库（77万词）三步匹配（精确/小写/去词形变化）给 data.js 补音标，5 分类覆盖率 97.2%（初中98.5%/高中97.3%/四级99.8%/六级100%/专业85.5%），新增 build-phonetic.js 脚本；② 🔊 发音功能：浏览器 Web Speech API（speechSynthesis），零外部服务，卡片正面/背面各一圆形蓝色渐变按钮，翻面自动朗读一次（200ms 延迟），lang=en-US rate=0.9；新增 speakWord() + case 'speak' 路由 + .speak-btn CSS。 |
| V1.09 | 2026-08-30 10:59 | 🐛 修复 🔊 发音静默：Chrome on macOS 的 Web Speech API 有静默 bug（voice 找到但不出声），彻底抛弃作为主方案，改用有道词典真人发音接口（dict.youdao.com/dictvoice?type=2&audio=单词，返回真人 MP3）；Web Speech 降级为离线兜底；双方案架构：有网→有道，离线→Web Speech，有道失败自动回退。 |
| V1.10 | 2026-08-30 11:13 | ⚡ 去掉翻面自动朗读：V1.08 设计了翻面自动朗读（200ms 延迟），但用户反馈安静环境下造成干扰；删除 flipCard() 内 setTimeout 调用，改为只有点击 🔊 按钮才发音。 |
| V1.11 | 2026-08-30 11:22 | 🐛 修复游标顺序推进仍重复抽词：根因是 pickWords 每次都 sort unlearned（hit 布尔值 + 不稳定排序），导致 hit 相同的词内部位置每次都变，游标指向的 index 对应不同的词；删掉 sort，data.js 已按 hit 排好序，保持原始顺序游标就稳定了。 |
| V1.12 | 2026-09-01 07:00 | ① 新增学习倒计时（紧迫感，默认 3 秒/词）：学习页右上角 SVG 圆形进度环（绿/橙/红三档色 + <30% 红色脉冲），环中心 mm:ss 倒计时数字，⏸ 手动暂停 + 切页 visibilitychange 自动暂停；时间到弹窗提示并跳结果页，剩余词不算学过；结果页新增「实际用时/目标用时/平均每词用时」行；② index.html 静态资源加 `?v=X.XX` 版本号参数，彻底绕过浏览器静态缓存，版本更新后不用手动硬刷新。 |
| V1.13 | 2026-09-01 07:31 | ① 🐛 修复🔊 双音源叠加 bug：原 speakWord 中 `audio.play().catch(()=>speakBySpeech)` 会在 Chrome 自动播放策略拦截/异常时误触发 Web Speech 兜底，导致「有道女声 + Web Speech 男声」同时播放；改为只有 `audio.onerror`（真正的网络/资源加载失败）才回退，并加 `fallbackFired` 去重锁 + 每次发声前 `speechSynthesis.cancel()` 清残留；② 倒计时每词时长从 3s 调整为 5s（SEC_PER_WORD 3→5），节奏更从容。 |
| V1.14 | 2026-09-01 08:57 | ⚙️ 倒计时每词时长 5s → 8s（首遍过单词表，节奏更宽松）；后续熟悉可下调回 5s/3s，仅需改 `SEC_PER_WORD` 常量一行。 |
| V1.15 | 2026-09-02 18:55 | 🏷️ 标签重命名：记得→认识、不太熟悉→眼熟、重点不变、必背不变、毕业→过关；只改 LABELS 的 name 显示值，key/权重/颜色不变，已存 localStorage 标签数据完全不受影响；app.js 内 14 处硬编码旧名称（注释、按钮文案、alert/confirm 提示）同步更新。 |
| V1.16 | 2026-09-04 20:27 | 🏷️ 标签微调：fuzzy 显示名从「眼熟」改为「模糊」；其余 key/颜色/权重不变；一行修改，数据零影响。 |
| V1.17 | 2026-09-05 07:32 | 🔍 新增「查词 + 查词本」功能：① 首页常驻搜索框，跨 5 分类精确匹配（大小写不敏感），命中后 count+1 自动收入查词本，结果卡片可直接打标签（同步原分类）；② 首页新增「查词本」卡片（第六分类），详情页按查词次数降序列表，可选 10/20/50/100/全部练习；③ 查词本练习按查词次数从多到少抽词，自动跳过已认识/过关的词，卡片显示所属分类+当前标签+查词次数；④ 标签全局同步（key 为 cat:word，查词本打标签=原分类打标签）；⑤ lookups 存入 localStorage（wa:lookups）并纳入备份/导入；⑥ 未收录的词提示不收入查词本。 |
| V1.18 | 2026-09-05 07:47 | 🔧 查词本调整：① 练习不再跳过已认识/过关的词——所有词都可复习/改标签（打了认识的以后也可能忘）；② 列表只显示查词次数最多的前 10 名，避免列表过长；③ 删除「清空查词本」按钮（历史记录保留更有价值）。 |
| V1.19 | 2026-09-05 08:08 | 🔊 查询结果卡片新增发音按钮：在单词旁加 🔊 按钮，点击调用有道真人发音接口（复用 speakWord），方便查词时顺便听发音。 |
| V1.20 | 2026-09-07 10:14 | 📚 新增「短语本」独立模块：① 独立 phrases.js 数据文件，410 条短语（初中 120 + 高中 180 + 开发工具 110）；② 首页新增短语本卡片，显示总数/已学/掌握度；③ 短语本详情页含 level 统计 + 数量按钮；④ 学习卡片复用现有标签系统（认识/模糊/重点/必背/过关）+ 倒计时（12s/条）+ 有道发音；⑤ 卡片显示 level（初中/高中/开发）+ 当前标签；⑥ 独立存储（wa:p-labels/p-studied/p-sessions/p-totals/p-cursor），纳入备份导出/导入；⑦ 游标顺序推进抽词，复用单词本同款逻辑。 |

---

## 一、产品定位

本地网页背单词程序（HTML + CSS + JS），双击或浏览器打开即用，无需安装。定位为**个人英语词汇强化工具**，覆盖初中到专业全阶段，核心目标是"每天背一点，长期坚持"。

设计原则：
- **简洁大方** — 界面清晰，让人有每天打开的冲动
- **强化记忆** — 记不清的词反复刷，熟词过滤掉
- **进度可控** — 能尽快过完一遍，也能专项复习
- **零依赖** — 数据存浏览器本地，关掉再开记录还在

---

## 二、词库与分类

### 2.1 五个分类

| 分类 key | 名称 | 词量 | 数据来源 | 音标 |
|---|---|---|---|---|
| `junior` | 初中 | 1886 | 必掌握单词清单_初中高中.md（T1 段） | 暂留空 |
| `senior` | 高中 | 1972 | 必掌握单词清单_初中高中.md（T2 段） | 暂留空 |
| `cet4` | 四级 | 1267 | CET4_edited.txt（去重后净增） | 自带（约 60%） |
| `cet6` | 六级 | 908 | CET6_edited.txt（去重后净增） | 全部自带 |
| `major` | 专业 | 689 | 领域技术词_补充牌组.md | 暂留空 |
| **合计** | | **6722** | | |

### 2.2 去重策略（分层完全去重）

同一个词只会出现在一个分类里，绝不重复：

1. **初中** — 原始数据，不去重
2. **高中** — 原始数据（已是初中基础上的新增，不含初中）
3. **专业** — 原始数据，独立
4. **四级** — 排除任何已在初中 / 高中 / 专业出现的词（共排除 3328 词）
5. **六级** — 排除任何已在初中 / 高中 / 专业 / 四级出现的词（共排除 1257 词）

去重 key 规则：单词小写化 → 去词性标注（如 `work(s)` → `work`）→ 去非字母字符。见 [build-data.js](file:///Users/zoujiean/TraeWork%20CN/背单词/build-data.js) 的 `wordKey` 函数。

### 2.3 数据生成

- 脚本：[build-data.js](file:///Users/zoujiean/TraeWork%20CN/背单词/build-data.js)
- 产物：[data.js](file:///Users/zoujiean/TraeWork%20CN/背单词/data.js)（挂载到 `window.WORDS`）
- 运行：`node build-data.js`
- 词资料更新后重跑脚本即可，无需改程序代码

每个词条结构：
```json
{
  "word": "abandon",
  "meaning": "vt.丢弃；放弃，抛弃",
  "phonetic": "əˈbændən",
  "example": "",
  "hit": true
}
```
- `hit` — 是否在个人语料中命中（用于排序优先级）

---

## 三、学习流程

### 3.1 整体路径

```
首页 → 选分类 → 选数量(10/20/50) → 卡片学习 → 结果页 → 再来一轮
                                  ↓
                            按标签复习（强化）
```

### 3.2 卡片学习

- **正面**：英文单词（+ 音标，若有）
- **看答案** → 翻面
- **背面**：中文释义（+ 例句，若有）+ 5 个标签按钮
- 打标签 → 自动进下一张，全部完成弹结果页

### 3.3 结果页

- 显示本轮各标签分布
- 提供"再背 20 / 50 个""返回分类"入口

---

## 四、标签系统

### 4.1 五个标签

| 标签 key | 名称 | 颜色 | 权重 | 行为 |
|---|---|---|---|---|
| `know` | 认识 | 绿 #34c759 | 0.3 | 会，但仍低频出现巩固 |
| `fuzzy` | 模糊 | 黄 #f5a623 | 3 | 常出现 |
| `key` | 重点 | 橙 #ff6b35 | 4 | 高频 |
| `must` | 必背 | 红 #e74c3c | 5 | 最高频 |
| `graduate` | 过关 | 蓝 #4f7cff | 0 | **彻底不再出现** |

### 4.2 标签机制要点

- **覆盖式**：每次打标签覆盖上一次，状态跟着最新判断走
- **过关即过滤**：标"过关"的词在日常抽词中被排除，词表越练越少
- **自动回炉**：过关词在"复习过关词"里若改标其他标签，自动回到训练池
- **排版**：前 4 个标签 2×2 网格，"过关"单独一行全宽按钮（文案带"· 不再出现"）

---

## 五、抽词逻辑

### 5.1 默认抽词（游标顺序推进，绝不回头）

见 [app.js](file:///Users/zoujiean/TraeWork%20CN/背单词/app.js) 的 `pickWords` 函数：

1. **过滤过关词** — 标"过关"的不参与
2. **拆分** — 未学过 vs 已学过
3. **未学词按频次降序** — 命中语料的在前，未命中的在后（保证高频实用词先遇到）
4. **游标顺序取词** — 用 `STORE.cursor[cat]` 游标从未学池下标 `[cursor, cursor+n)` 取连续 n 个词；取完 `cursor += n`；游标到末尾时归零重扫（此时未学池已因打标签变小）
5. **不足回退** — 未学词不足 n 时，用加权随机从已学词补充（必背/重点会被刷到）
6. **游标持久化** — `STORE.cursor[cat]` 存 localStorage（key `wa:cursor`），刷新/重启不丢

**设计要点**：完全去随机滑动窗口（原 V1.03 及之前 bug：随机 startIdx + 未学池收缩导致相邻轮次重叠）。游标推进保证推进速度稳定。

效果：每天背 20 个，全部是新词。1886 ÷ 20 ≈ 95 天过完初中一遍，无重叠。

**翻面即算已学（V1.04 起）**：`flipCard` 首次翻面时写 `STORE.studied[k] = 1`（之后打标签 `markLabel` 正常 `+1`，两者兼容）。防止背到一半退出时，已翻面看过的词仍留在未学池反复出现。

### 5.2 复习模式（强化记忆）

- **按标签复习** — 勾选标签（如只刷"必背+重点"），从对应词池随机抽
- **复习过关词** — 抽 20 个过关词巩固；复习时改标其他标签会自动回炉

### 5.3 权重表（加权随机时使用）

| 状态 | 权重 | 相对频率 |
|---|---|---|
| 必背 | 5 | 最高 |
| 重点 | 4 | 高 |
| 不太熟悉 | 3 | 中高 |
| 未学过 | 1 | 中 |
| 学过（无标签） | 0.6 | 低 |
| 记得 | 0.3 | 最低 |

必背被抽到概率 ≈ 记得的 16 倍。

---

## 六、过关机制

### 6.1 三件套

1. **过关规则** — 标"过关"的词被 `pickWords` 过滤，不再出现
2. **复习过关词** — 分类页按钮，从过关池抽 20 个复习；改标其他标签自动回炉
3. **重置过关** — 分类页按钮，一键清空该分类所有"过关"标签，全部放回训练池（有确认弹窗，只清标签不删学习记录）

### 6.2 设计意图

- 熟词过滤 → 词表越练越少，集中精力在生词
- 偶尔回收 → 主动触发，防彻底遗忘
- 一键重置 → "忘得差不多了，重新筛一遍"的场景

---

## 七、查词与查词本（V1.17）

### 7.1 设计背景

工作/阅读中遇到眼熟但想不起意思的单词，希望：① 快速查出释义；② 把这些"老记不住"的词收集起来专门练。

### 7.2 首页搜索

- 首页常驻搜索框，粘贴单词回车或点「搜索」
- 跨 5 个分类（初中/高中/四级/六级/专业）精确匹配（大小写不敏感）
- 命中后：显示单词、音标、释义、所属分类、当前标签、查词次数；**自动 count+1 收入查词本**
- 结果卡片可直接打标签（同步到原分类的标签存储）
- 未命中：提示「词库未收录」，不收入查词本

### 7.3 查词本（第六分类）

- 首页新增「🔍 查词本」卡片，显示已收集词数
- 详情页：按查词次数降序列表（最不熟的在前），显示单词、释义、所属分类、当前标签、查词次数
- 打了「认识/过关」的词列表中淡化显示，练习时自动跳过
- 可选 10 / 20 / 50 / 100 / 全部 开始练习
- 支持「清空查词本」（只清查词记录，不影响标签和学习进度）

### 7.4 查词本练习

- 抽词顺序：按查词次数从多到少（最不熟的先练）
- 所有词都参与练习（不跳过认识/过关，方便复习和改标签）
- 卡片上额外显示：所属分类（彩色）+ 当前标签 + 查词次数
- 打标签全局同步：查词本里改标签 = 原分类里改标签（key 为 `cat:word`）

### 7.5 数据存储

- `wa:lookups`：`{ "cat:word": { count: 次数, lastAt: 时间戳 } }`
- 纳入备份导出 / 导入恢复（collectData + importBackup）

---

## 八、统计与打卡

### 8.1 统计页

- 每分类：总词数 / 已学 / 完成轮数 / 累计练习词次 / 已过关数 / 各标签词数
- 整体：总词数 / 已学词数 / 累计打卡天数 / 连续打卡天数

### 8.2 连续打卡

- 每天完成一轮学习即打卡
- 连续天数递增，断一天归 1
- 首页右上角显示🔥连续天数

---

## 九、数据持久化

全部存浏览器 `localStorage`，key 前缀 `wa:`：

| key | 内容 | 结构 |
|---|---|---|
| `wa:labels` | 标签 | `{ "cat:word": "know\|fuzzy\|key\|must\|graduate" }` |
| `wa:studied` | 学习次数 | `{ "cat:word": 练习次数 }` |
| `wa:sessions` | 每分类完成轮数 | `{ junior: 0, senior: 0, cet4: 0, cet6: 0, major: 0 }` |
| `wa:totals` | 每分类累计词次 | 同上结构 |
| `wa:checkin` | 打卡 | `{ streak: 0, lastDate: '', dates: [] }` |
| `wa:autobackup` | 自动备份状态 | `{ lastAuto: ISO时间, count: 次数, handleReady: bool }` |
| `wa:cursor` | 每分类未学词游标 | `{ junior: 0, senior: 0, cet4: 0, cet6: 0, major: 0 }` |
| `wa:lookups` | 查词本记录 | `{ "cat:word": { count: 次数, lastAt: 时间戳 } }` |

另有 IndexedDB 库 `wordapp`（store `handles`）持久化 File System Access API 的 `fileHandle`，页面刷新后仍能恢复自动备份文件授权。

---

## 十、备份与恢复（V1.02 目录滚动 / V1.05 双触发点）

### 设计目标
- 防误操作：误删标签/误点重置时可恢复（自动备份多份历史）
- 防清缓存/换电脑：浏览器 localStorage 被清时仍可恢复（手动导出文件）
- 防单点失败：多份独立文件，某次备份失败或数据污染不影响历史备份

### 双重备份机制

| 机制 | 触发方式 | 存储位置 | 保留份数 | 文件名 |
|---|---|---|---|---|
| 自动备份 | 启动时 + 完成一轮时，双重检查（距上次 ≥1 天才写），每天最多 1 份 | 用户授权的备份目录（File System Access API 目录模式） | 滚动保留最近 7 份 | `wordapp-autobackup-YYYYMMDD-HHMM.json` |
| 手动导出 | 用户点按钮 | 本地文件（浏览器下载） | 每次新文件 | `wordapp-manual-YYYYMMDD.json` |

### 关键实现
- **目录授权**（Chrome/Edge 支持，Safari 不支持）：首次 `showDirectoryPicker` 让用户选备份目录（建议选"背单词备份"空文件夹），`directoryHandle` 存入 IndexedDB，页面刷新后仍能恢复。
- **每次新建文件**：备份时在目录里新建带时分时间戳的文件，不覆盖旧备份。
- **滚动删除**：保留最近 7 份，超出的最旧备份用 `removeEntry` 删除。
- **双触发点（V1.05 起）**：
  - ① App 启动时调 `checkAutoBackup()`（入口保证打开就有机会检查）
  - ② 完成一轮学习（advance 跳结果页前）再调 `checkAutoBackup()`（弥补"启动时距上次<24h、当天跨 24h 却没备份"的漏洞）
  - 两次调用内部均有 `now - last >= 24h` 判断，到点才写，**备份次数零增加，每天最多 1 份**
- **权限确认**：刷新后 Chrome 可能要求重新确认目录权限（不静默但不丢数据）。
- **导入恢复**：统计页"导入备份"按钮选 JSON 文件，覆盖当前所有学习记录（有确认弹窗）。
- **立即备份**：统计页按钮，随时手动触发一次备份写入。

### 备份内容
全部学习数据：labels / studied / sessions / totals / checkin / autoBackup。

### 入口
统计页底部「💾 备份与恢复」区块，显示自动备份状态、上次时间、次数，及 4 个按钮：手动导出 / 设置自动备份 / 导入备份 / 立即备份一次。

---

## 十一、文件清单

| 文件 | 作用 |
|---|---|
| [index.html](file:///Users/zoujiean/TraeWork%20CN/背单词/index.html) | 入口页面 |
| [style.css](file:///Users/zoujiean/TraeWork%20CN/背单词/style.css) | 样式 |
| [app.js](file:///Users/zoujiean/TraeWork%20CN/背单词/app.js) | 主逻辑（抽词/翻卡/标签/统计/打卡） |
| [data.js](file:///Users/zoujiean/TraeWork%20CN/背单词/data.js) | 单词数据（自动生成，勿手改） |
| [build-data.js](file:///Users/zoujiean/TraeWork%20CN/背单词/build-data.js) | 数据生成脚本 |
| [DESIGN.md](file:///Users/zoujiean/TraeWork%20CN/背单词/DESIGN.md) | 本设计文档 |

---

## 十一、后续待补

- [ ] 音标：初中/高中/专业暂留空，后续用开源词库（如 ECDICT）匹配补充
- [ ] 例句：全部分类暂留空，后续补充
- [ ] 间隔复习：过关词超过 N 天后自动少量回炉（类似 Anki）
- [ ] 每日目标：设定每日背词数量目标，未完成提醒
- [ ] 词库扩展：如需更多词，加考研 5500 词作为第六分类

---

## 附：访问方式

本地启动：`python3 -m http.server 8765`（在项目目录下）
访问地址：`http://localhost:8765/`
或直接双击 `index.html` 打开（部分浏览器 localStorage 限制时需用 http.server）

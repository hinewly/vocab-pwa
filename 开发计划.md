# 开发计划

| 项目 | 内容 |
|---|---|
| 作者 | 邹光凡 |
| 更新时间 | 2026-09-05 08:08 |

---

## 待开发

### 2. 单词整理（批量改标签 / 批量清理）

> 📌 **状态：需求草稿 · 待用户细化，暂未启动代码**
> 触发背景：用户第一遍过单词时，积累了大量打过「记得 / 不太熟悉 / 重点 / 必背 / 毕业」标签的词，希望有一个独立管理界面，一次性看清楚整个分类的分布，批量调整标签，让数据更准确精细。

#### 初步设计思路（V1.15，暂定）

- **入口**：分类详情页（选 10/20/50/100/200 按钮下方）加按钮「🗂 整理本分类单词」，进入独立整理页，不影响学习流程。
- **顶部筛选区**：
  - 标签按钮栏：`全部 / 记得 / 不太熟悉 / 重点 / 必背 / 毕业 / 未标记`（按真实计数），点哪个就只显示该类词。
  - 搜词框：支持按单词、音标、中文释义实时模糊过滤。
- **中部列表区**：
  - 表格列：`☑️多选 | 单词 | 音标 | 中文释义 | 当前标签`
  - 分页（每页 30-50 条），避免 1972 词一次加载卡顿
- **批量操作区**：选中 ≥1 条后出现操作按钮
  - 改标签：记得 / 不太熟悉 / 重点 / 必背 / 取消标签
  - 标记毕业（= 等价于"删除出学习流"，安全可恢复）
- **关于「删除」**：暂定不做 data.js 物理删除（前端无法改静态文件 + 删错不可恢复），用「毕业」标签实现等价过滤；毕业词只在"复习毕业词"里可见，学习流程完全不出现。
- **底部统计行**：`高中 共 1972 词 · 当前筛选显示 150 条 · 已选 3 条`

#### 待用户确认的问题（后续启动前确认）

1. **"删除"= 标记毕业**是否可接受？还是真的要从 data.js 源文件物理删除？
2. 筛选条件除了「按标签」+「搜词框」，是否还要加「只看未学过 / 只看已学过」？
3. 这个功能建议在「第一遍过完 + 所有词打了标签」之后做，效果最好；用户自己想什么时候启动？

---

### 1. 例句补充

- 当前状态：全部分类例句暂留空
- 方案：用开源词库（ECDICT 自带例句）或简单造句
- 难点：低-中，数据匹配 + 例句质量
- 展示：卡片背面中文下方显示例句
- 状态：待开发

---

## 已完成

### 学习倒计时（V1.12）

- 设计目标：给背单词带来紧迫感和专注度，默认 3s/词，根据数量自动计算目标时间
- 界面：学习页 topbar 右上角圆形进度环（绿 >60% / 橙 30-60% / 红 <30% 带脉冲动画），环内中心 mm:ss 倒计时数字，右侧 ⏸ 手动暂停按钮
- 暂停机制：手动点按钮 ⏸/▶ 切换；`visibilitychange` 监听切页面自动暂停/恢复，防止"偷偷耗时间"
- 时间到处理：alert 提示「已完成 X/Y，剩余不算学过」→ 直接跳结果页，剩余词不打 studied 标记，下次还会出现
- 结果页新增：⏱ 实际用时 / 目标用时 / 平均每词用时 行
- 配套优化：index.html 静态资源（style.css / data.js / app.js）加 `?v=1.12` 版本号参数，彻底绕过浏览器静态缓存，以后版本更新无需手动硬刷新
- 状态：已完成

### 掌握度展示（V1.03）

- 掌握度定义：毕业词 + 记得词 ÷ 总词数 × 100%
- 颜色分档 5 档：<50% 红 / 50-70% 橙 / 70-85% 黄 / 85-95% 蓝 / ≥95% 绿
- 目标值：初中 99% / 高中 95% / 四级 90% / 六级 90% / 专业 95%
- 展示位置：首页每个分类卡片显示掌握度百分比 + 彩色进度条 + 目标刻度线；统计页显示掌握度详情、已掌握词数、距目标差几个词
- 实时计算：打标签立即更新，无需等待
- 状态：已完成

### 抽词重复 bug 修复 & 翻面即算已学（V1.04）

- 根因：pickWords 用随机 startIdx + 未学池收缩，相邻轮次滑动窗口重叠，导致已处理的未学词在后续轮次重复出现
- 修复：改为 `STORE.cursor[cat]` 游标顺序推进，严格从未学池按频次降序下标取连续 n 个，游标前进 n，绝不回头；到末尾时归零重扫
- 游标持久化：localStorage `wa:cursor`，刷新不丢
- 翻面即算已学：flipCard 首次翻面写 `STORE.studied[k]=1`，之后 markLabel 正常 +1，防中途退出导致已翻面的词仍留未学池
- 文档：新增「更新日志.md」按时间线记录每次版本完整改动（含根因、影响范围），首页加「📝 更新日志」入口
- 状态：已完成

---

## 历史版本

| 版本 | 内容 |
|---|---|
| V1.0 | 五分类词库、卡片学习、5 标签、毕业机制、统计打卡 |
| V1.01 | 备份与恢复（手动导出 + 自动备份 + 导入） |
| V1.02 | 自动备份升级为目录滚动模式（每天 1 份，保留 7 份） |
| V1.03 | 掌握度展示（5 档颜色 + 目标进度条 + 实时计算） |
| V1.04 | 修复抽词重复（游标推进）、翻面即算已学、新增更新日志文档 |
| V1.05 | 自动备份新增"完成一轮后"检查触发点（双检查，防当天备份窗口期错过） |
| V1.06 | 备份专项验证与修复（collectData 补 cursor 导出、importBackup 补 cursor 还原、doAutoBackup count+1 前置保证自洽） |
| V1.07 | 抽词数量新增 100/200 立体渐变按钮（分类页/复习页/结果页同步） |
| V1.08 | 音标全量补全（ECDICT 匹配，覆盖率 97.2%）+ 🔊 发音功能（Web Speech API，按钮+翻面自动读） |
| V1.09 | 🐛 修复 🔊 发音静默（Chrome macOS Web Speech 已知 bug 静默），改用有道真人发音接口（MP3），Web Speech 降级为离线兜底 |
| V1.10 | ⚡ 去掉翻面自动朗读（安静环境下干扰），改为只有点击 🔊 按钮才发音 |
| V1.11 | 🐛 修复游标顺序推进仍重复抽词：pickWords 每次 sort unlearned（hit 布尔值+不稳定排序）导致游标 index 对应不同词，删掉 sort 保持 data.js 原始顺序 |
| V1.12 | ✨ 学习倒计时（紧迫感 3s/词 + 圆形进度环 + 双暂停 + 结果页用时统计）+ index.html 静态资源加 ?v=X.XX 版本号参数绕过浏览器缓存 |
| V1.13 | 🐛 修复🔊 双音源叠加（有道女声+Web Speech 男声并发）+ 倒计时节奏从 3s/词 调整为 5s/词 |
| V1.14 | ⚙️ 倒计时每词时长 5s → 8s（首遍过单词表节奏宽松，后续熟悉可调回） |
| V1.15 | 🏷️ 标签重命名：记得→认识、不太熟悉→眼熟、毕业→过关（只改 name 显示值，key/权重/颜色不变，已存数据不受影响） |
| V1.16 | 🏷️ fuzzy 标签显示名微调：眼熟→模糊（一行修改，数据零影响） |
| V1.17 | 🔍 查词 + 查词本：首页常驻搜索框跨 5 分类匹配，命中自动收入查词本并可直接打标签；新增查词本（第六分类），按查词次数降序专练老记不住的词，自动跳过已认识/过关的词；标签全局同步 |
| V1.18 | 🔧 查词本调整：练习不再跳过已认识/过关的词（所有词都可复习/改标签）；列表只显示查词次数前 10 名；删除「清空查词本」按钮 |
| V1.19 | 🔊 查询结果卡片新增发音按钮，查词时可直接听发音 |
| V1.20 | 📚 新增「短语本」独立模块：phrases.js 410 条短语（初中/高中/开发工具），独立存储+游标抽词+12s 倒计时+复用标签系统 |

---

# 📱 PWA 项目（vocab-pwa）

> 从原 TraeWork CN/背单词 V1.20 改造为 PWA，部署在 GitHub Pages。
> 仓库：https://github.com/hinewly/vocab-pwa
> 在线：https://hinewly.github.io/vocab-pwa/

## V1.0.0 — 首次 PWA 发布（2026-09-22）

基础架构：
- 从原项目 `/Users/zoujiean/TraeWork CN/背单词` 完整快照迁移（6722 词 + 410 短语）
- PWA 三件套：manifest + service-worker + icons
- 部署：GitHub Actions 上传 `public/` 到 GitHub Pages
- 多用户档案系统：13 个 localStorage key 加 profile 前缀（`wa:<id>:labels`）
- 一次性迁移：自动把 V1.20 老数据搬到"用户1"档案
- 联系邮箱反馈（首页底部 mailto 链接）
- README 中英双语

## V1.0.1 — 首页 UI 优化（2026-09-22）

- 首页 5 张分类卡 → 2 列网格布局（紧凑版）
- 新增 `compactCard()` 统一模板函数
- 新增 3 张导航卡（学习统计 / 开发计划 / 更新日志），合并到 `.cats-grid`
- 修版本号徽章对比度（白字 + 透明 → 半透明白底 + 深绿字）

## V1.0.2 — 改名交互 + 卡分组 + 邮箱修复（2026-09-22）

- 首页分两组（"📚 学习" / "🛠️ 系统"）+ 系统卡差异化样式（虚线 border + 浅灰底 + › 箭头）
- 改名功能改行内编辑（替代不可靠的 `prompt()`）
- profile-chip 对比度修复（跟版本号同款问题）
- footer 邮箱用 JS 运行时拼接（绕过 GitHub Pages 自动 obfuscate）
- AGENTS.md 加"用户联系方式"规则
- 更新日志页面接入 CHANGELOG.md

## V1.1.0 — 词缀功能（**进行中** ⏳）

数据层 ✅：
- 新增 `data-affixes.js`（精选 132 条词缀 = 57 前缀 + 75 后缀，差 8 条到 140）
- 新增 5 个存储 key：`wa:a-labels/a-studied/a-sessions/a-totals/a-cursor`
- 加辅助函数：`affixAll()` / `affixKey()` / `affixTotal()` / `affixLabelOf()` / `affixMastery()`

UI / 路由 ❌（待补）：
- 首页加"📐 词缀"卡
- `#/affix` 路由（词缀总览页）
- `#/affix-study/N` 路由（学习页）
- `renderAffix()` / `renderAffixStudy()` 函数
- `go-affix` / `flip-affix` / `label-affix` action cases
- 数据补到 140 条

## 🚧 待定事项（用户思考中）

### 🔄 PWA 与浏览器标签数据同步问题

**问题描述**（2026-09-22 提出）：
- 已装到桌面的 Chrome PWA 和浏览器普通标签，localStorage 互相隔离
- 用户在 PWA 查了一个词（查词本 1 词），但浏览器标签还是 0 词
- 两侧用户名都是 `hinewly`（都跑了同名迁移），但学习数据各自积累

**根因**：
- localStorage 按 origin 分区，但 PWA 安装方式 / Chrome 配置文件 / 无痕模式可能让两侧落入不同分区
- 不是代码 bug，是浏览器架构限制

**待选方案**（用户决定中）：
- A. 维持现状（用户只用 PWA，浏览器当预览）
- B. 加状态条提醒：检测到数据不一致时显示 ⚠️
- C. 加一键"导出 → 导入"同步按钮
- D. 真云同步（后端 + 账号）

**触发条件**：用户考虑好了选哪个再做，**当前不动**。
